[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "traceplan"
version = "0.1.0"
description = "Software observational platform analysis"
authors = [{ name = "Daniel Beard" }]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
plan = "traceplan.cli:app"